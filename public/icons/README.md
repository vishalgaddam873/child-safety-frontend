# PWA Icons

Add the following icon files for the PWA manifest:

- `icon-192.png` – 192×192 px
- `icon-512.png` – 512×512 px

You can generate them from a single square image (e.g. your app logo) using any image editor or online tool. Place the files in this directory.
